#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
	ifstream fi("PTIT.in");
	ofstream fo("PTIT.out");

    string s;
    while (getline(fi, s)) {
        fo << s << endl;
    }
    return 0;
}
